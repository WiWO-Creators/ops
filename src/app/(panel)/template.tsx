/**
 * Next exige que el template sea el `default` de este archivo; la transicion en si vive en el
 * componente compartido, para que el panel y el portal no se separen con el tiempo.
 */
export { TransicionDePagina as default } from '@/componentes/estructura/TransicionDePagina'
